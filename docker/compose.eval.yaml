name: sparkclaw-eval

services:
  evaluator:
    image: alpine:3.22
    working_dir: /workspace
    volumes:
      - ..:/workspace
    entrypoint: ["sh", "-c", "apk add --no-cache bash curl jq >/dev/null && bash scripts/run-eval.sh"]
